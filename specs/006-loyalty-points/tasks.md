# Tasks: Loyalty Points for Receipt Upload**Input**: Design documents from `/specs/006-loyalty-points/`**Prerequisites**: plan.md, spec.md, research.md, data-model.md, contracts/me-loyalty.md, quickstart.md**Tests**: Included — Constitution Article III mandates unit tests for use cases and integration tests for critical endpoints (confirm receipt, loyalty endpoint).**Organization**: Tasks grouped by user story. US numbering follows `spec.md`:- US1 (P1) — Earn points for a confirmed receipt- US3 (P1) — Query point balance and history- US2 (P2) — Earn bonus points for valuable contributions(US1 and US3 are both Priority 1 in the spec. US1 is scheduled firstbecause its award logic produces the data US3 queries; US2 layers ontothe US1 award path and is scheduled last.)## Format: `[ID] [P?] [Story] Description`- **[P]**: Can run in parallel (different files, no dependencies)- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3)- Include exact file paths in descriptions---## Phase 1: Setup (Shared Infrastructure)**Purpose**: Configuration plumbing for the loyalty feature. No behavioralchange yet — all tasks here are additive and cannot break existing tests.- [ ] T001 Add loyalty configuration fields to `internal/config/config.go`: `LOYALTY_BASE_POINTS` (default 10), `LOYALTY_FIRST_OBSERVATION_BONUS` (default 5), `LOYALTY_DATA_COMPLETION_BONUS` (default 3), `LOYALTY_DAILY_AWARD_CAP` (default 20). Loaded via `caarlos0/env/v11` like the existing fields.- [ ] T002 [P] Add sanity unit test for new config defaults in `internal/config/config_test.go` (new file): verifies defaults and that overrides parse correctly.- [ ] T003 [P] Add reason-code constants in a new file `internal/usecase/loyalty_reasons.go`: `ReasonReceiptConfirmed`, `ReasonFirstObservationProduct`, `ReasonFirstObservationStore`, `ReasonDataCompletion`, `ReasonDailyLimitReached`. Documented in `data-model.md`.---## Phase 2: Foundational (Blocking Prerequisites)**Purpose**: Schema + port interface that ALL user stories depend on.⚠️ No user story work can begin until this phase is complete.- [ ] T004 Create migration `migrations/000005_loyalty_receipt_link.up.sql`: `ALTER TABLE loyalty_transactions ADD COLUMN receipt_id UUID REFERENCES receipts(id) ON DELETE SET NULL;` then `CREATE UNIQUE INDEX uniq_loyalty_tx_receipt ON loyalty_transactions(receipt_id) WHERE receipt_id IS NOT NULL;` then `CREATE INDEX idx_loyalty_tx_user_day ON loyalty_transactions(user_id, created_at);`.- [ ] T005 Create the matching down migration `migrations/000005_loyalty_receipt_link.down.sql`: drop the two indexes then `ALTER TABLE loyalty_transactions DROP COLUMN receipt_id;`.- [ ] T006 Extend the `entities.LoyaltyTransaction` struct in `internal/domain/entities/loyalty_transaction.go` (new file): copy the existing definition from `user.go` and add `ReceiptID *string` so award rows can carry the receipt pointer; keep `user.go` re-exporting the type for backward compatibility (or move and re-export) without breaking the existing scan in `internal/adapter/postgres/user_repository.go` — add the new column scan.- [ ] T007 Update `internal/adapter/postgres/user_repository.go` `RecentTransactions` to also `SELECT receipt_id` (nullable) and scan into the new `ReceiptID *string` field; keep backward-compatible column order or re-scan all columns.- [ ] T008 Define a new port interface `LoyaltyRepository` in `internal/domain/ports/loyalty_repository.go` with methods:  - `AwardForReceipt(ctx, userID, receiptID string, points int, reason string) error` — inserts a `loyalty_transactions` row with the receipt_id, updates `users.points` atomically, and conflicts on the unique index (returns a sentinel `ports.ErrAlreadyAwarded`).  - `DailyGrantCount(ctx, userID string) (int, error)` — counts distinct `receipt_id` with `points > 0` for the user on the current UTC day.  - `Balance(ctx, userID string) (int, error)` — returns `users.points`.  - `History(ctx, userID string, limit int) ([]entities.LoyaltyTransaction, error)` — returns movements ordered by `created_at DESC`.  - Also add `paternalErrAlreadyAwarded = errors.New("loyalty: already awarded")` to the same file.- [ ] T009 Define a new port `FirstObservationChecker` in `internal/domain/ports/first_observation.go` with method `PreviouslyObserved(ctx, productID, storeID string) (bool, error)` returning true if a `price_observations` row for that (product, store) pair already exists.**Checkpoint**: Foundation ready — ports and schema are in place; user story implementation can begin.---## Phase 3: User Story 1 — Earn points for a confirmed receipt (Priority: P1) 🎯 MVP**Goal**: A user who confirms a receipt earns the base points exactly once; resubmission grants nothing further.**Independent Test**: Quickstart Scenario A — confirm a new receipt, observe `balance == LOYALTY_BASE_POINTS` and one history entry; confirm the same receipt again, balance unchanged.### Tests for User Story 1 (TDD — write first, watch them fail)- [ ] T010 [P] [US1] Unit test `internal/usecase/loyalty_award_test.go`: `TestLoyaltyAward_BaseOnceAwarded` — fake `LoyaltyRepository` and `FirstObservationChecker`; expect one `AwardForReceipt` call with `reason == ReasonReceiptConfirmed` and `points == LOYALTY_BASE_POINTS`; second call with same receipt ID returns `ports.ErrAlreadyAwarded` and asserts the use case swallows it gracefully (no error propagated to caller).- [ ] T011 [P] [US1] Unit test for daily cap: `TestLoyaltyAward_DailyLimit` — fake repo returns `DailyGrantCount == cap`; expect `AwardForReceipt` called with `points == 0` and `reason == ReasonDailyLimitReached`; `users.points` is NOT incremented by the use case's expected call.- [ ] T012 [P] [US1] Integration test `internal/adapter/http/receipt_confirm_handler_test.go` (extend if exists; otherwise append) — asserting that confirming a receipt transitions the status to `CONFIRMED` and that a follow-up GET `/me/loyalty` shows one movement (use a fake/stub `LoyaltyRepository`).### Implementation for User Story 1- [ ] T013 Implement `internal/usecase/loyalty_award.go`:  - `LoyaltyAwardUseCase` struct with deps: `ports.LoyaltyRepository`, `ports.FirstObservationChecker`, plus config values (base points, first-obs bonus, data-completion bonus, daily cap) injected via constructor.  - `AwardForReceipt(ctx, receipt entities.Receipt, payload entities.ConfirmPayload, observations []entities.PriceObservation)`:    1. If `receipt.Status != CONFIRMED` → returns nil silently (FR-012).    2. Count daily grants; if `< cap` compute base; else `points = 0`, `reason = ReasonDailyLimitReached`, build reason slice.    3. Build a single reason slice `[]string{ReasonReceiptConfirmed}`, sum points = base; (US2 will append bonuses here).    4. Call `loyaltyRepo.AwardForReceipt(ctx, userID, receiptID, points, strings.Join(reasons, ";"))`. If `ErrAlreadyAwarded`, return nil silently (FR-004). Other errors: log and return nil (award never aborts confirm per R-07).- [ ] T014 Implement `internal/adapter/postgres/loyalty_repository.go`:  - `NewLoyaltyRepository(pool)` constructor.  - `AwardForReceipt`: open a tx; `INSERT INTO loyalty_transactions (user_id, receipt_id, points, reason, created_at) VALUES ($1, $2::uuid, $3, $4, NOW())` — relies on unique partial index for idempotency; on `unique_violation` (code 23505) → rollback + return `ports.ErrAlreadyAwarded`. On success: `UPDATE users SET points = points + $3, updated_at = NOW() WHERE id = $1`. Commit.  - `DailyGrantCount`: `SELECT COUNT(DISTINCT receipt_id) FROM loyalty_transactions WHERE user_id = $1 AND points > 0 AND created_at >= date_trunc('day', NOW())`. (date_trunc at UTC since column is TIMESTAMPTZ.)  - `Balance`: `SELECT points FROM users WHERE id::text = $1`.  - `History`: `SELECT id::text, user_id::text, points, reason, created_at, receipt_id::text FROM loyalty_transactions WHERE user_id::text = $1 ORDER BY created_at DESC LIMIT $2`; scan into `entities.LoyaltyTransaction{ReceiptID *string}`.- [ ] T015 Wire `LoyaltyAwardUseCase` into `cmd/api/main.go`: instantiate `postgres.NewLoyaltyRepository(pool)`, build the use case with `cfg.LoyaltyBasePoints ... DailyAwardCap`, and pass it to `usecase.NewReceiptConfirmUseCase` (extend its constructor signature).- [ ] T016 Modify `internal/usecase/receipt_confirm.go` to accept and invoke the `LoyaltyAwardUseCase`: after a successful `recompute.Execute` and BEFORE `events.EmitReceiptConfirmed`, call `loyaltyAward.AwardForReceipt(ctx, receipt, payload, observations)`. The receipt entity and `payload` are already available; fetch the latest receipt via `repo.GetByID` if not in hand, so the award sees the freshly-set `CONFIRMED` status. Update its existing unit test `receipt_confirm_test.go` to inject and assert on a fake `LoyaltyAwardUseCase` (or fake the new deps).- [ ] T017 Implement `internal/adapter/postgres/first_observation_repository.go` satisfying `ports.FirstObservationChecker`: `SELECT EXISTS(SELECT 1 FROM price_observations WHERE product_id = $1::uuid AND store_id = $2::uuid)`. Wire it in `main.go` (US2 will use it; US1 wires the dependency now even if the use case ignores it for now).**Checkpoint**: US1 complete — confirming a receipt awards base points exactly once; resubmission does not double-award. `go test ./internal/usecase ./internal/adapter/http ./internal/adapter/postgres` is green.---## Phase 4: User Story 3 — Query balance and history (Priority: P1)**Goal**: The user (and only the authenticated user) can see their point balance and full ordered history via `GET /api/v1/me/loyalty`.**Independent Test**: Quickstart Scenarios E, F, G — consistent balance+history, 401 on no token, no cross-user leak.### Tests for User Story 3 (TDD — write first, watch them fail)- [ ] T018 [P] [US3] Unit test `internal/usecase/loyalty_query_test.go`: `TestLoyaltyQuery_ConsistentBalance`, `TestLoyaltyQuery_EmptyHistory`, `TestLoyaltyQuery_OrdersByCreatedAtDesc`. Use a fake `LoyaltyRepository` returning canned movements and assert the projected DTO shape matches the contract.- [ ] T019 [P] [US3] Integration test `internal/adapter/http/loyalty_handler_test.go`:  - `TestLoyaltyHandler_Unauthenticated` — no `Authorization` header → 401, body `{ "error": "invalid or expired token" }`.  - `TestLoyaltyHandler_OK` — valid token → 200, body `{ balance, history: [...] }`.  - `TestLoyaltyHandler_CrossUser` — token for user B never sees user A's movements (fake repo asserts userID taken from context, not request).  - `TestLoyaltyHandler_MethodNotAllowed` — POST → 405.### Implementation for User Story 3- [ ] T020 [P] [US3] Create `internal/usecase/loyalty_query.go`:  - `LoyaltyQueryUseCase` struct depends on `ports.LoyaltyRepository`.  - DTOs matching `contracts/me-loyalty.md`: `LoyaltyResponse{ Balance int ; History []LoyaltyMovementOut }`, `LoyaltyMovementOut{ ID, Reason, CreatedAt (RFC3339 string), ReceiptID *string }`.  - `GetLoyalty(ctx, userID) (*LoyaltyResponse, error)`: returns `Balance` from `repo.Balance`, `History` from `repo.History(userID, 100)`; empty slice (not nil) when no rows.- [ ] T021 [P] [US3] Create `internal/adapter/http/loyalty_handler.go`:  - `GetLoyalty(w, r)` handler mirrors `profile_handler.GetPoints` structure: reject non-GET with 405; read userID from JWT context; on missing userID return 401; on use case error 500; otherwise 200 with the JSON DTO.  - Reuse `writeJSON` / `writeError` helpers already used by `profile_handler.go`.- [ ] T022 [US3] Register the route in `internal/adapter/http/router.go` under the authenticated v1 group: `authed.Get("/me/loyalty", loyaltyHandler.GetLoyalty)`. Extend `NewRouter` signature to accept a `*LoyaltyHandler` (add as a parameter, do not break existing test wiring — update existing call site in `main.go` and existing `router_test_helpers_test.go` accordingly).- [ ] T023 [US3] Wire `LoyaltyHandler` in `cmd/api/main.go`: instantiate `usecase.NewLoyaltyQueryUseCase(loyaltyRepo)` and `httpapi.NewLoyaltyHandler(...)`; pass into `NewRouter`.**Checkpoint**: US3 complete — `GET /api/v1/me/loyalty` returns the authenticated user's balance and ordered history, rejects anonymous and cross-user access.---## Phase 5: User Story 2 — Bonus points for valuable contributions (Priority: P2)**Goal**: First-observation and data-completion bonuses are awarded on top of the base, recorded as part of the same award row's reason slice (per data-model.md note about compactness).**Independent Test**: Quickstart Scenarios C and D — first observation adds the bonus delta to the points and includes the matching reason token; data completion behaves likewise; neither is granted in non-qualifying cases.### Tests for User Story 2 (TDD — write first, watch them fail)- [ ] T024 [P] [US2] Unit test `internal/usecase/loyalty_award_test.go` (extend): `TestLoyaltyAward_FirstObservationProduct` — fake `FirstObservationChecker.PreviouslyObserved` returns false for one observation → `points == base + firstObsBonus`, reason contains `first_observation_product`.- [ ] T025 [P] [US2] Unit test: `TestLoyaltyAward_FirstObservationStore` — fl/fake store-creation signal (use a small helper `AwardForReceipt` argument `storeCreatedNow bool` derived in `receipt_confirm.go` from the resolve-create behavior) → reason contains `first_observation_store`.- [ ] T026 [P] [US2] Unit test: `TestLoyaltyAward_DataCompletion` — payload fully populated → reason contains `data_completion`; omit one optional field → no `data_completion` token.- [ ] T027 [P] [US2] Unit test: `TestLoyaltyAward_BonusesRespectDailyLimit` — when daily capped, `points == 0` regardless of bonuses, reason == `daily_limit_reached` only (bonuses are NOT separately recorded).### Implementation for User Story 2- [ ] T028 [US2] Extend `LoyaltyAwardUseCase.AwardForReceipt` in `internal/usecase/loyalty_award.go` to compute bonuses (only when under the daily cap):  1. For each observation: if `!firstObservationChecker.PreviouslyObserved(ctx, obs.ProductID, obs.StoreID)` append `ReasonFirstObservationProduct` and add the bonus once per distinct qualifying pair (deduplicate within the same award).  2. If the caller signals a store was newly created (extend the method signature with `StoreCreatedNow bool`), append `ReasonFirstObservationStore` and add the bonus once.  3. Data completion: if `payload.PurchaseDate != ""` AND `payload.Total > 0` AND every item has non-nil `quantity`, `unit_price`, `currency` — append `ReasonDataCompletion` and add the bonus.  4. Sum `points = base + bonuses`; join reasons with `";"`.- [ ] T029 [US2] Modify `internal/usecase/receipt_confirm.go` to detect and pass `StoreCreatedNow` to the award use case. Use a small helper: ask `repo.ResolveOrCreateStore` and inspect whether a pre-check query finds an existing store id (alternatively, extend `ReceiptRepository.ResolveOrCreateStore` to return `(storeID string, created bool, error)` — preferred to avoid a second query; adjust the existing usages with `_, _ = created` discards where not needed).- [ ] T030 [US2] Wire the `FirstObservationChecker` (already created in T017) into `LoyaltyAwardUseCase` constructor in `cmd/api/main.go`. Confirm `main.go` builds.**Checkpoint**: US2 complete — bonuses correctly granted and correctly withheld. All quickstart scenarios A–G pass.---## Phase 6: Polish & Cross-Cutting Concerns**Purpose**: Verification and small consolidations affecting multiple stories.- [ ] T031 [P] Run `dart analyze`/linting analog: `go vet ./...` and `go test ./...` from repo root; fix any flagged issue. (Constitution Art. III.)- [ ] T032 [P] Update `internal/adapter/http/router_test_helpers_test.go` and any existing router test for the new `NewRouter` signature; update router unit tests if present.- [ ] T033 [P] Verify architecture gates: `go test ./internal/domain ./internal/usecase` — the `TestDomainHasNoInfrastructureImports` test must still pass; confirm new files in `internal/domain/ports/` import only stdlib + `ahorrapp/internal/domain/entities`.- [ ] T034 [P] Verify `users.points == SUM(loyalty_transactions.points)` invariant in the existing `user_repository_test.go` if present (or add a small pgx-based integration test in `internal/adapter/postgres/loyalty_repository_test.go` that runs against the local Postgres only when the `DATABASE_URL` env var is set, skipping otherwise — matches the project's existing convention if it has one).- [ ] T035 Run `quickstart.md` validation scenarios A–G manually against `docker compose up` stack; ensure each passes.- [ ] T036 [P] Update `docs/` if there is a running API doc listing endpoints (search for `/api/v1/users/me/points` references; add `/api/v1/me/loyalty` next to it, leaving the legacy endpoint documented as backward-compatible).- [ ] T037 [P] Verify no Flutter changes required by this feature (the legacy endpoint remains compatible); document in plan/tasks notes.---## Dependencies & Execution Order### Phase Dependencies- **Setup (Phase 1)** — no dependencies; T001 first (config), T002/T003 parallel.- **Foundational (Phase 2)** — depends on Setup; T004/T005 migration pair first, then T006-T009 ports; once done, all user stories can begin.- **US1 (Phase 3)** — depends on Foundational only.- **US3 (Phase 4)** — depends on Foundational only; can run in parallel with US1 if allocated separately (different files), or after US1 sequentially.- **US2 (Phase 5)** — depends on US1 (extends its award logic and use case file).- **Polish (Phase 6)** — depends on all stories being functionally complete.### User Story Dependencies- US1: independent after Foundational.- US3: independent after Foundational — only needs `LoyaltyRepository.Balance/History`, which T008 provides.- US2: builds ON US1 (extends `loyalty_award.go` and the `AwardForReceipt` call site in `receipt_confirm.go`); cannot start before US1's award wiring exists.### Within Each User Story- Tests written first (TDD per Constitution Art. III) and must fail before implementation.- Port → Postgres adapter → use case → handler → router wiring → main wiring.- Story complete (tests green) before next priority.### Parallel Opportunities- Foundational: T004 ping-pongs naturally with T005; T006-T009 are file-disjoint and all `[P]`.- US1 tests T010-T012 are `[P]` (different files).- US1 impl T013 (use case) and T017 (postgres port) are file-disjoint `[P]` — but must both land for the wiring T015/T016 to compile; treat as parallel writes that integrate together.- US3 T018/T019/T020/T021 are all `[P]` (different files).- US2 tests T024-T027 are `[P]`.- Polish T031-T037 mostly `[P]` (different files).---## Parallel Example: Foundational + US1 kickoff```text# After Phase 1:Task T004: Create migration up        migrations/000005_loyalty_receipt_link.up.sqlTask T005 [P]: Create migration down  migrations/000005_loyalty_receipt_link.down.sqlTask T006 [P]: Extend entity          internal/domain/entities/loyalty_transaction.goTask T007 [P]: Update user repo scan  internal/adapter/postgres/user_repository.goTask T008 [P]: LoyaltyRepository port internal/domain/ports/loyalty_repository.goTask T009 [P]: FirstObsChecker port   internal/domain/ports/first_observation.go# Once Phase 2 is in:Task T010 [P] [US1]: unit test        internal/usecase/loyalty_award_test.goTask T013     [US1]: use case         internal/usecase/loyalty_award.goTask T014     [US1]: postgres impl    internal/adapter/postgres/loyalty_repository.goTask T017 [P] [US1]: first-obs pg     internal/adapter/postgres/first_observation_repository.go```---## Implementation Strategy### MVP First (US1 only)1. Phase 1 Setup → Phase 2 Foundational → Phase 3 US1.2. **STOP and VALIDATE**: Quickstart Scenario A (and B for daily cap).3. Reaching this MVP already produces a usable loyalty flywheel.### Incremental Delivery4. Add US3 → Quickstart Scenarios E/F/G validate the read side.5. Add US2 → Quickstart Scenarios C/D validate the bonuses.6. Polish phase for cross-cutting cleanups.### Single-implementer sequencing (this repo)Since this is an agent-driven single-implementer setup, execute phasessequentially: 1 → 2 → 3 → 4 → 5 → 6, exploiting the `[P]` tasks withineach phase where multiple file edits can be batched. Commit after eachstory checkpoint (Constitution Art. II implies small, reviewable steps).---## Notes- All Go files use package paths already imported by the project; no new external dependencies introduced (Constitution Art. VI.1, simplicity).- The Flutter app is intentionally not changed in this feature (legacy endpoint preserved).- Each `[P]` task must NOT edit the same file as another uncommitted `[P]` task in the same batch — when two `[P]` tasks target the same file (e.g., `internal/usecase/loyalty_award.go` being extended by US2 after US1 introduces it), they are sequential despite being checked `[P]` for file-LOC disjointness; the parallel mark should be interpreted per-phase only.